# Flow steps (living document)

Add each screen **in the order it usually appears**. For each step, note:

- **When**: what you see (title, heading, URL hint, button text).
- **Do**: what automation should do (click X, type Y).

We will mirror this list in `flow.js` as `flowSteps[]`.

---

## Steps

| # | Page / state | When (how we know) | Action |
|---|----------------|-------------------|--------|
| 1 | *(pending)* | — | — |

---

*Next: describe Step 1 in chat; we add the row here and the matching block in `flow.js`.*
